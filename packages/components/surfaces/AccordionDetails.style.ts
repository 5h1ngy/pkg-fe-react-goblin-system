import styled from 'styled-components'

export const AccordionDetailsRoot = styled.div`
  padding: ${({ theme }) => `${theme.spacing(0.5)} ${theme.spacing(2.6)} ${theme.spacing(2.2)}`};
  color: ${({ theme }) => theme.palette.text.secondary};
`
