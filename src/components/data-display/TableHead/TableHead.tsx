import type { TableHeadProps } from './TableHead.types'
import { TableHeadRoot } from './TableHead.style'

export const TableHead = (props: TableHeadProps) => <TableHeadRoot {...props} />

TableHead.displayName = 'TableHead'

